# 开发 & 部署使用说明

> 项目当前部署在 **GitHub Pages**：<https://chxxx.github.io/gs-mobile/>
> 本文档说明：代码仓库的位置、`git push` 行为、以及"改代码 → 自动部署"的完整流程。

---

## 1. 仓库地址与 remote 一览

在项目根目录执行 `git remote -v` 可以看到当前所有远程仓库：

| remote   | 地址                                                        | 用途                                   |
| -------- | ----------------------------------------------------------- | -------------------------------------- |
| `github` | `ssh://git@ssh.github.com:443/chxxx/gs-mobile.git`          | **主仓库 / 部署源**（每天开发就推这里） |
| `gitee`  | `https://gitee.com/chxxx/gs-mobile.git`                     | 旧托管（保留，一般不再用）             |
| `origin` | `https://ghfast.top/https://github.com/huggingface/gsplat.js.git` | **上游原版库镜像，只读参考，禁止 push** |

> ⚠️ 本机全局 git 配置有一条 `url.https://ghfast.top/... .insteadOf=https://github.com/`，
> 它会**把所有 `https://github.com/...` 地址重写为 ghfast.top 只读镜像**。
> 因此：**请统一使用 `github` remote（已配好 SSH over 443）推送，不要手写 `https://github.com/...` 去 push**。

---

## 2. 现在 `git push` 会推到哪？

已把分支的上游（upstream）设好，**不带参数的 `git push` 行为如下**：

| 所在分支            | `git push` 会推到                    | 效果 |
| ------------------- | ------------------------------------ | ---- |
| `cleanup/resume-prep` | GitHub `cleanup/resume-prep` 分支    | **触发自动部署** |
| `main`              | GitHub `main` 分支                   | **触发自动部署** |
| `pages`             | （track 仍指向 gitee，见下方警告）    | 不要手动操作 |

### 建议的明确写法（不依赖所在分支）

```bash
# 推到 GitHub 主开发分支（最常用，会触发自动部署）
git push github cleanup/resume-prep

# 同步更新 GitHub main
git push github main

# 推到 Gitee 旧仓库（仅当你需要同步旧托管时）
git push gitee cleanup/resume-prep
```

> ❗ `origin` 永远不要 push —— 那是上游 gsplat.js 的只读镜像。
> ❗ 本地还有个 `pages` 分支（内容是 Gitee 时代的旧部署产物），**不要把它当部署目标使用**；
> GitHub 上的 `pages` 分支是 CI 自动生成并强制覆盖的。

---

## 3. 修改代码 → 自动部署的标准流程

GitHub Pages 监听 `pages` 分支；而 `pages` 分支由 **GitHub Actions**（`.github/workflows/deploy-pages.yml`）自动重建。
所以你**永远不用手动提交 pages**，只需要：

```bash
# 1) 在写代码的分支上提交改动（当前开发分支 cleanup/resume-prep）
git add -A
git commit -m "feat: ..."

# 2) 推到 GitHub（关键一步，触发 Actions 自动构建部署）
git push github cleanup/resume-prep
```

推送后 GitHub 会自动执行：

1. **Actions**（仓库 Actions 页可看到进度）：
   `npm ci` → `npm run site:build`（产出 `site-dist/`）→ 把产物**强制单提交推送到 `pages` 分支根目录`
2. **Pages** 检测到 `pages` 分支更新 → 自动重新发布

约 **1–3 分钟**后访问 <https://chxxx.github.io/gs-mobile/> 即为新版本。
若想手动触发一次（不改代码也重新部署）：GitHub → Actions → "Build & Deploy to GitHub Pages" → **Run workflow**。

---

## 4. 本地开发与验证

```bash
# 本地起开发服务器（根目录 index.html + demo.ts，即 GitHub Pages 展示的同一个应用）
npm run dev

# 按生产方式构建静态站到 site-dist/，验证产物
npm run site:build
npm run site:preview     # 本地预览 site-dist/（部署前的冒烟）
```

> `npm run build`（不带 site）是**库模式构建**（产出 npm 包用的 `dist/`），与 Pages 部署无关。

---

## 5. 场景（3DGS 演示模型）相关操作

场景清单由根目录 `scenes.json` 控制，模型放在 `scenes/` 目录，构建时会自动复制进站点。

当前 3 个场景：

```json
[
  { "name": "DrJohnson (r7 QPLY)", "file": "scenes/point_cloud_quantised_half_r7-drjohnson.ply" },
  { "name": "Garden (r7 QPLY)",    "file": "scenes/point_cloud_quantised_half_r7-garden.ply" },
  { "name": "Truck (r7 QPLY)",     "file": "scenes/point_cloud_quantised_half_r7-truck.ply" }
]
```

- 页面打开后下拉框默认高亮第一个场景；**访客手动选择后才开始渲染**（不自动加载）。
- **换模型**：覆盖 `scenes/` 下对应 .ply → 提交推送即可（保持文件名或同步改 `scenes.json`）。
- **加/删场景**：改 `scenes.json`（`name` 为下拉显示名，`file` 为相对站点的路径）。
- ⚠️ **单文件不能超过 100 MB**（GitHub 硬限制）；超过需压缩/降采样，或用 Git LFS（暂未启用）。

---

## 6. 注意事项 / 常见坑

| 坑 | 说明 |
| -- | ---- |
| 裸 `git push` 前确认所在分支 | 已把 upstream 指向 GitHub，正常情况裸 push = 推 GitHub；若曾被 gitee 影响过请用 `git push github ...` |
| `origin` 是上游镜像 | 它是 huggingface/gsplat.js 的只读代理，**不要 push** |
| `https://github.com/...` 会被改写 | 本机 `insteadOf` 全局规则会把它们改到 ghfast.top（只读），push 一律用 `github` remote |
| GitHub 单文件 >100 MB 会被拒 | 历史里曾出现 109 MB 的 `public/point_cloud.ply` 导致 main 推不上去，已通过重定向 main 解决；以后不要再提交 >100 MB 文件 |
| 不要在本地 `pages` 分支手工开发/提交 | GitHub 的 `pages` 每次 CI 都会 force 覆盖，手工改动会丢 |
| 场景 .ply 很大（约 42 MB 总量） | push 时会比平时慢，属正常；Git 会对未变化内容去重 |
| `gitee` 仓库/Gitee Pages 仍在 | 未删除，仅停止使用；如需归档可在 Gitee 端自行处理 |

---

## 7. 完整链路示意

```text
你本地开发（cleanup/resume-prep）
   │  git push github cleanup/resume-prep
   ▼
GitHub 仓库
   ├─ cleanup/resume-prep（源码，最新）
   ├─ main（对齐到最新源码）
   └─ pages（部署产物，CI 自动维护）
   │
   ▼  GitHub Actions（.github/workflows/deploy-pages.yml）
   npm ci → npm run site:build → force push 产物到 pages
   │
   ▼  GitHub Pages 监听 pages 分支
https://chxxx.github.io/gs-mobile/
```

---

## 8. 为什么 `git push` 之后就能自动部署？

这套机制由 **3 个各自独立、又互相咬合的服务**组成，全都在 GitHub 云端，不需要你本地做任何打包/上传。

### ① push 触发「事件」

你的命令 `git push github cleanup/resume-prep` 本质只是：把本地代码上传到 GitHub 仓库的分支上。
但 GitHub 会**监听每次 push**，并检查仓库里有没有「规则文件」说要在 push 时做事——
这个规则文件就是 `.github/workflows/deploy-pages.yml`（你每次推送代码时都会一并推上去，所以规则永远跟着代码走）。

### ② GitHub Actions 执行「自动流水线」

deploy-pages.yml 里写明了触发器（`on: push` 到 main / cleanup/resume-prep）和要做的事（jobs/steps）：

| 步骤 | 相当于 | 在哪里跑 |
| ---- | ------ | -------- |
| `actions/checkout` | git clone 你的代码 | GitHub 临时虚拟机（Ubuntu） |
| `npm ci` | 安装依赖 | 同上 |
| `npm run site:build` | 用 Vite 把 index.html + demo.ts + scenes 打包成纯静态文件到 `site-dist/` | 同上 |
| `git push --force ... HEAD:pages` | 把 `site-dist/` 内容作为 **pages 分支**的一次性新提交，强推回仓库 | 同上（用 GitHub 自动签发的临时 token，不需要你的密码） |

也就是说：**你在本地要手动做的「打包 + 传产物到 pages 分支」，现在由云端虚拟机自动完成了。**

### ③ GitHub Pages 发布「pages 分支」

GitHub Pages 本身是一个纯静态托管服务。你在仓库 Settings → Pages 里把它设置为「**Source: pages 分支，根目录**」——
从此它每次检测到 `pages` 分支有了新提交，就会自动把那一整棵树发布到公网（约几十秒~1 分钟），无需手动刷新。

### 串联起来看

```text
git push github cleanup/resume-prep
        │  （上传源码，仅此一个动作是你做的）
        ▼
GitHub 检测到 push → 检查 .github/workflows/deploy-pages.yml 的触发条件是否匹配
        │
        ▼ 匹配 → GitHub Actions 在云端 Ubuntu 上自动执行
  checkout → npm ci → npm run site:build（产出 site-dist/）
        │
        ▼
  Actions 把 site-dist/ 强推为 pages 分支的新提交（用自动 token，无需密码）
        │
        ▼
GitHub Pages 发现 pages 分支更新 → 自动发布
        │
        ▼
https://chxxx.github.io/gs-mobile/  更新完成（1~3 分钟）
```

### 几个关键设计点

- **为什么「打包结果自动 push 到 pages」是可靠的**：deploy-pages.yml 声明了 `permissions: contents: write`，即允许该工作流写回本仓库的 pages 分支——这是仓库内部授权，不涉及你的账号密码。
- **为什么 pages 每次都是"一次性的新提交"**：Actions 在 site-dist 目录里临时 `git init` → `git add -A` → `git commit` → `push --force HEAD:pages`。所以 pages 分支永远只有"最新那一次站点"这一条提交，干净且不会随每次构建积累历史。
- **为什么页面失败/回滚容易**：pages 分支当前提交即线上版本；代码出问题只需再 push 一次修复，或回滚 pages 分支到上一个提交。
- **同一次 push 里的两个 workflow**：`deploy-pages.yml`（负责发布）和 `test.yml`（负责 lint/test 质量门禁）会并行跑——前者让页面更新，后者给你检查红灯/绿灯，互不干扰。

### 你要记住的唯一心智模型

> **push 代码 = 自动重建并发布网站。**
> 你只负责：改代码 → commit → `git push github cleanup/resume-prep`。
> 剩下的打包、产物回写、发布，全部由 GitHub 云端接力完成。

